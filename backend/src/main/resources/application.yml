spring:
  application:
    name: cbt-platform

  # Database configuration
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/cbt_platform}
    username: ${DB_USERNAME:cbt_user}
    password: ${DB_PASSWORD:cbt_password}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # JPA configuration
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
        temp:
          use_jdbc_metadata_defaults: false
    open-in-view: false

  # Flyway configuration
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  # Redis configuration
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 2000
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  # Jackson configuration
  jackson:
    serialization:
      write-dates-as-timestamps: false
      indent-output: true
    deserialization:
      fail-on-unknown-properties: false
    default-property-inclusion: non_null

# JWT configuration
jwt:
  secret: ${JWT_SECRET:cbt-platform-secret-key-change-in-production-min-256-bits}
  access-token-expiration: 86400000  # 24 hours
  refresh-token-expiration: 604800000 # 7 days

# Server configuration
server:
  port: ${PORT:8080}
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: false

# Logging configuration
logging:
  level:
    root: INFO
    com.cbt.platform: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# Actuator configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

# API Documentation (Swagger)
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

# LLM Configuration
llm:
  # Default provider to use
  default-provider: claude

  # Retry configuration
  retry:
    max-attempts: 3
    backoff-delay: 1000  # milliseconds

  # Providers configuration
  providers:
    # Claude (Anthropic)
    claude:
      enabled: ${LLM_CLAUDE_ENABLED:false}
      api-key: ${CLAUDE_API_KEY:}
      base-url: https://api.anthropic.com
      model: ${CLAUDE_MODEL:claude-3-5-sonnet-20241022}
      max-tokens: 1024
      temperature: 0.7
      timeout: 30000

    # OpenAI (GPT)
    openai:
      enabled: ${LLM_OPENAI_ENABLED:false}
      api-key: ${OPENAI_API_KEY:}
      base-url: https://api.openai.com/v1
      model: ${OPENAI_MODEL:gpt-4o}
      max-tokens: 1024
      temperature: 0.7
      timeout: 30000

    # Google Gemini
    gemini:
      enabled: ${LLM_GEMINI_ENABLED:false}
      api-key: ${GEMINI_API_KEY:}
      base-url: https://generativelanguage.googleapis.com
      model: ${GEMINI_MODEL:gemini-1.5-pro}
      max-tokens: 1024
      temperature: 0.7
      timeout: 30000

    # Local LLM (Ollama)
    local:
      enabled: ${LLM_LOCAL_ENABLED:false}
      base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
      model: ${OLLAMA_MODEL:llama3}
      max-tokens: 1024
      temperature: 0.7
      timeout: 60000
